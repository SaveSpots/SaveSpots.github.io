# Expo/Metro and EAS builds need a flat node_modules — React Native's resolver
# does not follow pnpm's symlinked layout.
#
# pnpm v10+ reads this from `nodeLinker:` in pnpm-workspace.yaml; this entry is
# only a fallback for pnpm v9 and older. Keep the two in sync.
node-linker=hoisted
